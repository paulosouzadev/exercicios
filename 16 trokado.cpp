#include<stdio.h>
#include<conio.h>
main()
{
      int x[16],i;
      for(i=0;i<=15;i++)
      {
                        printf("digite os valores");
                        scanf("%d",&x[i]);
      }
      for(i=8;i<=15;i++)
      {
                        scanf("%d",&x[i]);
      }
      for(i=0;i<=7;i++)
      {
                       x[i];
                       printf("%d",x[i]);
      }
      getch();
}
                        
